#include "stdio.h"
#include "nonstd.h"

// generate 2 million random numbers in range of 0-1023.
// expected distribution: 2000 per number (all number should be generated around 2000 times)
// result is printed in log, enable "MINH" in debug.h
int random1() {
    checkRandomPRA();
    return 0;
}